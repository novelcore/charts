apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: d1-kubeorg-azure-tenant-id-immutable
  annotations:
    kubecore.io/catalog-domain: "D1"
    kubecore.io/catalog-id: "d1-kubeorg-azure-tenant-id-immutable"
    kubecore.io/evidence-ref: "api/v1beta1/kubeorg_types.go"
    policies.kyverno.io/category: "KubeCore Catalog"
    policies.kyverno.io/description: "Azure AD Tenant ID is the directory the subscription is anchored to. Changing it breaks every workload identity binding."
    policies.kyverno.io/severity: "high"
    policies.kyverno.io/subject: "schema.kubecore.io/KubeOrg"
spec:
  validationFailureAction: Enforce
  failurePolicy: Fail
  background: true
  rules:
  - name: d1-kubeorg-azure-tenant-id-immutable
    match:
      any:
      - resources:
          kinds:
            - KubeOrg
    preconditions:
      all:
      - key: "{{ request.operation }}"
        operator: Equals
        value: UPDATE
    validate:
      message: "Field spec.azureConfig.tenantId on KubeOrg is immutable (catalog: d1-kubeorg-azure-tenant-id-immutable, domain: D1). See PRD-310."
      deny:
        conditions:
          all:
          - key: "{{ request.object.spec.azureConfig.tenantId || '' }}"
            operator: NotEquals
            value: "{{ request.oldObject.spec.azureConfig.tenantId || '' }}"
          - key: "{{ request.oldObject.spec.azureConfig.tenantId || '' }}"
            operator: NotEquals
            value: ""

