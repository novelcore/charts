{{- if .Values.crd.enable }}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  labels:
    {{- include "chart.labels" . | nindent 4 }}
  annotations:
    {{- if .Values.crd.keep }}
    "helm.sh/resource-policy": keep
    {{- end }}
    controller-gen.kubebuilder.io/version: v0.18.0
    graph.kubecore.io/relationships: |
      - target: KubeOrg
        field: spec.kubeOrgRef
        relationship: references
        cardinality: "*:1"
        required: true
        description: "KubePool references a KubeOrg for organization-level configuration"
      - target: KubeProject
        relationship: creates
        cardinality: "1:*"
        description: "KubePool creates KubeProject resources for project management"
    graph.kubecore.io/hierarchy-label: kubecore.io/kubepool
    graph.kubecore.io/xr-mappings: |
      - xrKind: XEKS
        apiVersion: platform.kubecore.io/v1alpha1
        cardinality: "1:1"
        description: "EKS cluster for the pool (AWS)"
      - xrKind: XGKE
        apiVersion: platform.kubecore.io/v1alpha1
        cardinality: "1:1"
        description: "GKE cluster for the pool (GCP)"
      - xrKind: XAks
        apiVersion: platform.kubecore.io/v1alpha1
        cardinality: "1:1"
        description: "AKS cluster for the pool (Azure)"
      - xrKind: XEksKubeSystem
        apiVersion: platform.kubecore.io/v1alpha1
        cardinality: "1:1"
        description: "Platform system infrastructure for the pool (AWS)"
      - xrKind: XGkeKubeSystem
        apiVersion: platform.kubecore.io/v1alpha1
        cardinality: "1:1"
        description: "Platform system infrastructure for the pool (GCP)"
      - xrKind: XAksKubeSystem
        apiVersion: platform.kubecore.io/v1alpha1
        cardinality: "1:1"
        description: "Platform system infrastructure for the pool (Azure)"
      - xrKind: XClusterOperators
        apiVersion: platform.kubecore.io/v1alpha1
        cardinality: "1:1"
        description: "In-cluster operators (ArgoCD, Crossplane, Traefik, Zot, ...) for the pool"
      - xrKind: XObservability
        apiVersion: platform.kubecore.io/v1alpha1
        cardinality: "1:1"
        description: "Observability stack (VictoriaMetrics) for the pool"
      - xrKind: XMLSystem
        apiVersion: platform.kubecore.io/v1alpha1
        cardinality: "1:1"
        description: "Pool-level ML system components (AWS, optional)"
      - xrKind: XGcpObservabilityCost
        apiVersion: platform.kubecore.io/v1alpha1
        cardinality: "1:1"
        description: "GCP cost/observability exporter for the pool (optional)"
      - xrKind: XInterconnect
        apiVersion: platform.kubecore.io/v1alpha1
        cardinality: "1:*"
        description: "Inter-pool network interconnect (optional)"
  name: kubepools.schema.kubecore.io
spec:
  group: schema.kubecore.io
  names:
    kind: KubePool
    listKind: KubePoolList
    plural: kubepools
    singular: kubepool
  scope: Namespaced
  versions:
    - additionalPrinterColumns:
        - jsonPath: .status.phase
          name: Phase
          type: string
        - jsonPath: .spec.kubeOrgRef
          name: KubeOrg
          type: string
        - jsonPath: .status.cloudProvider
          name: Cloud Provider
          type: string
        - jsonPath: .status.region
          name: Region
          type: string
        - jsonPath: .status.clusterType
          name: Cluster Type
          type: string
        - jsonPath: .status.kubernetesVersion
          name: K8s Version
          type: string
        - jsonPath: .metadata.creationTimestamp
          name: Age
          type: date
      name: v1beta1
      schema:
        openAPIV3Schema:
          description: KubePool is the Schema for the kubepools API
          properties:
            apiVersion:
              description: |-
                APIVersion defines the versioned schema of this representation of an object.
                Servers should convert recognized schemas to the latest internal value, and
                may reject unrecognized values.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
              type: string
            kind:
              description: |-
                Kind is a string value representing the REST resource this object represents.
                Servers may infer this from the endpoint the client submits requests to.
                Cannot be updated.
                In CamelCase.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
              type: string
            metadata:
              type: object
            spec:
              description: KubePoolSpec defines the desired state of KubePool
              properties:
                cloudProvider:
                  description: |-
                    CloudProvider holds the cloud-provider-specific cluster configuration.
                    Exactly one sub-spec (e.g., aws) must be set.
                  properties:
                    aws:
                      description: |-
                        AWS holds AWS-specific cluster configuration. Set this field to provision
                        an EKS cluster on Amazon Web Services. Mutually exclusive with other providers.
                      properties:
                        cluster:
                          description: Cluster defines the EKS cluster configuration.
                          properties:
                            compute:
                              description: |-
                                Compute defines the catalog of CPU/GPU compute classes available to ML
                                KubeApps on this cluster. See MLComputeConfig for semantics. AWS-side
                                per-class Karpenter NodePool emission is a follow-up; the field is
                                honoured by the type system today so KubeApp validation can pin
                                classes that exist in the cluster catalog.
                              properties:
                                mlClasses:
                                  description: |-
                                    MLClasses is the list of CPU and GPU classes the cluster exposes.
                                    The operator provisions one NodePool per class; idle classes scale
                                    to zero nodes.

                                    DEPRECATED (PRD-1124): pool-level ML compute is superseded by each ml
                                    environment's declared flavours (KubeProject environments[].ml sizes,
                                    rendered per environment by kubenv). Leave this empty on new pools.
                                    An empty list provisions NO pool-level ML step pools — the former
                                    kubecorecpu/kubecoregpu fallback pair is retired (#1134).
                                  items:
                                    description: |-
                                      MLComputeClass declares one named compute class in the cluster catalog.
                                      The Name becomes both the GKE NodePool name and the value of the
                                      platform.kubecore.io/nodegroup-type label that KubeApps target via
                                      nodeSelector. The Tier (cpu | gpu) drives default labels and toleration
                                      requirements on the WorkflowTemplate.

                                      The CEL rule below is the data-integrity guard for #357/#361: spot
                                      pricing (preemptible:true) is admitted ONLY on classes explicitly
                                      declared computeProfile: ephemeral-training. Because computeProfile
                                      defaults to stateful-serving, any class that sets preemptible:true
                                      without an explicit ephemeral-training profile is REJECTED at admission
                                      — including pre-existing classes from before this field existed, which
                                      is the exact 2026-06-03 preemptible-reclaim hazard. gke.go enforces the
                                      same invariant defensively even if admission is bypassed.
                                    properties:
                                      computeProfile:
                                        default: stateful-serving
                                        description: |-
                                          ComputeProfile declares the data-integrity posture of workloads that
                                          run on this class, which gates whether spot (preemptible) pricing is
                                          allowed:
                                            - "ephemeral-training": stateless/checkpointed jobs that tolerate
                                              mid-run eviction (the ONLY profile permitted to set
                                              preemptible:true).
                                            - "stateful-serving": serving pods holding model-weight / cache PVCs
                                              whose eviction loses in-flight or local state (#361).
                                            - "stateful-database": single-instance datastores (e.g. CNPG)
                                              where preemption + write-in-flight risks corruption / total
                                              loss (#357).
                                          Defaults to "stateful-serving" so an un-annotated class can never be
                                          scheduled on preemptible nodes — safety is the pool-wide default.
                                        enum:
                                          - ephemeral-training
                                          - stateful-serving
                                          - stateful-database
                                        type: string
                                      description:
                                        description: |-
                                          Description is shown in dashboard tooltips and in KubeApp validation
                                          error messages. Keep it short — "n2-standard-8 (8 vCPU, 32 GiB) —
                                          feature engineering / dataset transform".
                                        maxLength: 200
                                        type: string
                                      diskSizeGb:
                                        default: 50
                                        description: |-
                                          DiskSizeGb pins the per-node boot disk size. Bump for GPU
                                          training (model checkpoints + dataset cache fill quickly).
                                        format: int32
                                        maximum: 2000
                                        minimum: 20
                                        type: integer
                                      guestAccelerator:
                                        description: |-
                                          GuestAccelerator is required when Tier="gpu" on GCP. Its presence
                                          and validity is checked by the operator's reconciling phase.
                                        properties:
                                          count:
                                            description: |-
                                              Count is the number of GPUs per node. Most classes use 1; multi-
                                              GPU classes (e.g. 8×A100) use larger values bound by the machine
                                              type's spec.
                                            format: int32
                                            maximum: 16
                                            minimum: 1
                                            type: integer
                                          type:
                                            description: |-
                                              Type names the GCP accelerator (e.g. "nvidia-l4",
                                              "nvidia-tesla-t4", "nvidia-tesla-a100", "nvidia-h100-80gb").
                                              Must match GCP's accelerator catalog for the chosen MachineType.
                                            pattern: ^nvidia-[a-z0-9-]+$
                                            type: string
                                        required:
                                          - count
                                          - type
                                        type: object
                                      machineType:
                                        description: |-
                                          MachineType is the GCP machine type (e.g. "n2-standard-8",
                                          "g2-standard-4", "a2-highgpu-1g"). On AWS this field doubles as
                                          the EC2 instance type ("m6i.large", "g5.xlarge"); the operator
                                          adapter validates against the cloud's published catalog.
                                        minLength: 2
                                        type: string
                                      maxNodeCount:
                                        default: 4
                                        description: |-
                                          MaxNodeCount caps the autoscale ceiling for this class. Defaults
                                          to 4 — small enough to prevent runaway spend, large enough for
                                          most pipelines.
                                        format: int32
                                        maximum: 64
                                        minimum: 1
                                        type: integer
                                      name:
                                        description: |-
                                          Name is the developer-facing class name (e.g. "cpu-balanced",
                                          "gpu-l4-small"). Becomes the NodePool name and the
                                          platform.kubecore.io/nodegroup-type label. Must be DNS-1035
                                          compatible (lowercase, digits, hyphens; starts with a letter,
                                          ends with a letter or digit).
                                        maxLength: 40
                                        minLength: 2
                                        pattern: ^[a-z][a-z0-9-]*[a-z0-9]$
                                        type: string
                                      preemptible:
                                        description: |-
                                          Preemptible enables spot-tier pricing (~70% discount, eviction
                                          risk). Only set true on classes that explicitly handle preemption
                                          in their workflow steps. Admission rejects preemptible:true unless
                                          ComputeProfile is "ephemeral-training" (#357/#361).
                                        type: boolean
                                      tier:
                                        description: |-
                                          Tier is "cpu" or "gpu". Drives default labels/taints and
                                          determines whether GuestAccelerator is required.
                                        enum:
                                          - cpu
                                          - gpu
                                        type: string
                                    required:
                                      - machineType
                                      - name
                                      - tier
                                    type: object
                                    x-kubernetes-validations:
                                      - message: preemptible:true is only allowed when computeProfile is ephemeral-training (stateful-serving/stateful-database classes must run on-demand to avoid data loss on preemption)
                                        rule: '!has(self.preemptible) || !self.preemptible || self.computeProfile == ''ephemeral-training'''
                                  maxItems: 32
                                  type: array
                              type: object
                            kubernetesVersion:
                              default: "1.35"
                              description: KubernetesVersion specifies the Kubernetes version for the cluster
                              pattern: ^[0-9]+\.[0-9]+$
                              type: string
                            system:
                              description: System defines configuration for the system node group (kubecoresys)
                              properties:
                                capacityType:
                                  default: SPOT
                                  description: CapacityType specifies the capacity type for system nodes
                                  enum:
                                    - ON_DEMAND
                                    - SPOT
                                  type: string
                              type: object
                            tags:
                              additionalProperties:
                                type: string
                              description: Tags are additional tags to apply to the EKS cluster
                              type: object
                            type:
                              description: Type specifies whether this is a shared or dedicated cluster
                              enum:
                                - shared
                                - dedicated
                              type: string
                          required:
                            - kubernetesVersion
                            - type
                          type: object
                        region:
                          description: |-
                            Region is the AWS region where the EKS cluster will be created.
                            Must match one of the regions configured in the referenced KubeOrg.
                          pattern: ^[a-z]{2}-[a-z]+-[0-9]{1}$
                          type: string
                      required:
                        - cluster
                        - region
                      type: object
                    azure:
                      description: |-
                        Azure holds Azure-specific cluster configuration. Set this field to
                        provision an AKS cluster on Microsoft Azure. Mutually exclusive with
                        other providers.
                      properties:
                        cluster:
                          description: Cluster defines the AKS cluster configuration.
                          properties:
                            autoScalerProfile:
                              description: |-
                                AutoScalerProfile overrides the cluster autoscaler's scale-down knobs.

                                Leave unset to take the operator default (PRD-COST-001 D-07): the three
                                scale-down settings are written explicitly for clusterMode "dev" and left
                                entirely to the AKS RP for "production". Any field set here wins over the
                                operator default; fields left empty in production mode are not written at
                                all, so Azure's own defaults still apply to them.

                                Unlike GKE's single autoscalingProfile enum, AKS exposes the individual
                                autoscaler timings; only the three scale-down knobs are surfaced here.
                              properties:
                                scaleDownDelayAfterAdd:
                                  description: |-
                                    ScaleDownDelayAfterAdd is how long after a scale-up the autoscaler
                                    resumes evaluating scale-down. A whole number plus one unit —
                                    ms, s, m or h — e.g. "10m", "90s", "500ms", "2h".
                                    Operator default in dev mode: 10m.
                                  pattern: ^[0-9]+(ms|s|m|h)$
                                  type: string
                                scaleDownUnneeded:
                                  description: |-
                                    ScaleDownUnneeded is how long a node must sit unneeded before it becomes
                                    eligible for scale-down. A whole number plus one unit —
                                    ms, s, m or h — e.g. "10m", "90s", "500ms", "2h".
                                    Operator default in dev mode: 10m.
                                  pattern: ^[0-9]+(ms|s|m|h)$
                                  type: string
                                scaleDownUtilizationThreshold:
                                  description: |-
                                    ScaleDownUtilizationThreshold is the utilisation level (requested
                                    resources / capacity) below which a node is considered for scale-down,
                                    as a decimal fraction between 0 and 1, e.g. "0.5".
                                    Operator default in dev mode: 0.5.
                                  pattern: ^0\.[0-9]+$
                                  type: string
                              type: object
                            kubernetesVersion:
                              default: "1.35"
                              description: KubernetesVersion specifies the AKS version.
                              pattern: ^[0-9]+\.[0-9]+$
                              type: string
                            private:
                              description: Private enables private cluster mode (API server not exposed publicly).
                              type: boolean
                            system:
                              description: System defines configuration for the system node pool (kubecsys per CON-AZ-03).
                              properties:
                                machineType:
                                  description: |-
                                    MachineType specifies the Azure VM size for system nodes.
                                    Default (operator-side): Standard_D2ds_v5.
                                    Must be a Ddsv5 (or compatible local-SSD) family for ephemeral OS disk.
                                  type: string
                                preemptible:
                                  description: |-
                                    Preemptible overrides whether the system pool uses Spot priority. When
                                    nil, the operator default applies (false / on-demand — system addons are
                                    eviction-sensitive). Set explicitly only when the operator default is
                                    not appropriate for your subscription's quota or stability needs.
                                  type: boolean
                              type: object
                            tags:
                              additionalProperties:
                                type: string
                              description: Tags are additional Azure tags to apply to the AKS cluster.
                              type: object
                            tools:
                              description: |-
                                Tools defines configuration for the KubeCore tools node pool
                                (kubecoresys). Presence of this field opts the cluster into the
                                dual-pool architecture (D-AZ-12-7). When nil, legacy single-pool
                                layout is used.
                              properties:
                                machineType:
                                  description: |-
                                    MachineType specifies the Azure VM size for tools nodes.
                                    Default (operator-side): Standard_D4ds_v5.
                                    Must be a Ddsv5 (or compatible local-SSD) family for ephemeral OS disk.
                                  type: string
                                maxNodes:
                                  default: 2
                                  description: MaxNodes is the maximum number of nodes (autoscaler ceiling).
                                  format: int32
                                  minimum: 1
                                  type: integer
                                minNodes:
                                  default: 1
                                  description: MinNodes is the minimum number of nodes (autoscaler floor).
                                  format: int32
                                  minimum: 1
                                  type: integer
                                preemptible:
                                  description: |-
                                    Preemptible overrides whether the tools pool uses Spot priority. When
                                    nil, the operator default applies (true / spot — dev-mode cost
                                    optimization). Set false to use on-demand capacity (e.g. when the
                                    subscription's LowPriorityCores quota is insufficient).
                                  type: boolean
                              type: object
                            type:
                              description: Type specifies whether this is a shared or dedicated cluster.
                              enum:
                                - shared
                                - dedicated
                              type: string
                          required:
                            - kubernetesVersion
                            - type
                          type: object
                        region:
                          description: Region is the Azure region where the AKS cluster will be created (e.g., westeurope).
                          minLength: 1
                          type: string
                      required:
                        - cluster
                        - region
                      type: object
                    gcp:
                      description: GCP holds GCP-specific cluster configuration. Mutually exclusive with other providers.
                      properties:
                        cluster:
                          description: Cluster defines the GKE cluster configuration.
                          properties:
                            autoscalingProfile:
                              description: |-
                                AutoscalingProfile overrides the cluster-level GKE autoscaler profile.

                                Leave unset to take the operator default (PRD-COST-001 D-06): BALANCED
                                for clusterMode "dev", OPTIMIZE_UTILIZATION for "production".
                                OPTIMIZE_UTILIZATION drains an under-utilised node in ~1-2 minutes,
                                BALANCED after ~10 — the former reclaims quota fast but churns nodes on
                                bursty clusters, so dev pools pay repeated cold starts for savings they
                                never realise. Set this only to escape the default: BALANCED on a
                                production pool that scales too aggressively, or OPTIMIZE_UTILIZATION on
                                a dev pool that is genuinely quota-starved.

                                There is deliberately no kubebuilder default — "unset" is the signal the
                                operator uses to apply the clusterMode-derived value, and an admission
                                default would erase it.
                              enum:
                                - BALANCED
                                - OPTIMIZE_UTILIZATION
                              type: string
                            compute:
                              description: |-
                                Compute defines the cluster's ML compute classes (PRD-247 D-03 follow-up).
                                Declare exactly one cpu-tier and one gpu-tier class; each becomes a
                                dedicated GKE NodePool that autoscales from 0 — no nodes when idle.
                                These two classes are the SINGLE source of truth for ML pipeline compute:
                                the operator projects the cpu+gpu pair into each ML KubeApp's pipeline
                                context. A pipeline step routes to the gpu class iff its {step}-gpu submit
                                param is >0, else the cpu class — the engineer sets the request on the
                                Argo Submit form. KubeApps declare no class selection.
                              properties:
                                mlClasses:
                                  description: |-
                                    MLClasses is the list of CPU and GPU classes the cluster exposes.
                                    The operator provisions one NodePool per class; idle classes scale
                                    to zero nodes.

                                    DEPRECATED (PRD-1124): pool-level ML compute is superseded by each ml
                                    environment's declared flavours (KubeProject environments[].ml sizes,
                                    rendered per environment by kubenv). Leave this empty on new pools.
                                    An empty list provisions NO pool-level ML step pools — the former
                                    kubecorecpu/kubecoregpu fallback pair is retired (#1134).
                                  items:
                                    description: |-
                                      MLComputeClass declares one named compute class in the cluster catalog.
                                      The Name becomes both the GKE NodePool name and the value of the
                                      platform.kubecore.io/nodegroup-type label that KubeApps target via
                                      nodeSelector. The Tier (cpu | gpu) drives default labels and toleration
                                      requirements on the WorkflowTemplate.

                                      The CEL rule below is the data-integrity guard for #357/#361: spot
                                      pricing (preemptible:true) is admitted ONLY on classes explicitly
                                      declared computeProfile: ephemeral-training. Because computeProfile
                                      defaults to stateful-serving, any class that sets preemptible:true
                                      without an explicit ephemeral-training profile is REJECTED at admission
                                      — including pre-existing classes from before this field existed, which
                                      is the exact 2026-06-03 preemptible-reclaim hazard. gke.go enforces the
                                      same invariant defensively even if admission is bypassed.
                                    properties:
                                      computeProfile:
                                        default: stateful-serving
                                        description: |-
                                          ComputeProfile declares the data-integrity posture of workloads that
                                          run on this class, which gates whether spot (preemptible) pricing is
                                          allowed:
                                            - "ephemeral-training": stateless/checkpointed jobs that tolerate
                                              mid-run eviction (the ONLY profile permitted to set
                                              preemptible:true).
                                            - "stateful-serving": serving pods holding model-weight / cache PVCs
                                              whose eviction loses in-flight or local state (#361).
                                            - "stateful-database": single-instance datastores (e.g. CNPG)
                                              where preemption + write-in-flight risks corruption / total
                                              loss (#357).
                                          Defaults to "stateful-serving" so an un-annotated class can never be
                                          scheduled on preemptible nodes — safety is the pool-wide default.
                                        enum:
                                          - ephemeral-training
                                          - stateful-serving
                                          - stateful-database
                                        type: string
                                      description:
                                        description: |-
                                          Description is shown in dashboard tooltips and in KubeApp validation
                                          error messages. Keep it short — "n2-standard-8 (8 vCPU, 32 GiB) —
                                          feature engineering / dataset transform".
                                        maxLength: 200
                                        type: string
                                      diskSizeGb:
                                        default: 50
                                        description: |-
                                          DiskSizeGb pins the per-node boot disk size. Bump for GPU
                                          training (model checkpoints + dataset cache fill quickly).
                                        format: int32
                                        maximum: 2000
                                        minimum: 20
                                        type: integer
                                      guestAccelerator:
                                        description: |-
                                          GuestAccelerator is required when Tier="gpu" on GCP. Its presence
                                          and validity is checked by the operator's reconciling phase.
                                        properties:
                                          count:
                                            description: |-
                                              Count is the number of GPUs per node. Most classes use 1; multi-
                                              GPU classes (e.g. 8×A100) use larger values bound by the machine
                                              type's spec.
                                            format: int32
                                            maximum: 16
                                            minimum: 1
                                            type: integer
                                          type:
                                            description: |-
                                              Type names the GCP accelerator (e.g. "nvidia-l4",
                                              "nvidia-tesla-t4", "nvidia-tesla-a100", "nvidia-h100-80gb").
                                              Must match GCP's accelerator catalog for the chosen MachineType.
                                            pattern: ^nvidia-[a-z0-9-]+$
                                            type: string
                                        required:
                                          - count
                                          - type
                                        type: object
                                      machineType:
                                        description: |-
                                          MachineType is the GCP machine type (e.g. "n2-standard-8",
                                          "g2-standard-4", "a2-highgpu-1g"). On AWS this field doubles as
                                          the EC2 instance type ("m6i.large", "g5.xlarge"); the operator
                                          adapter validates against the cloud's published catalog.
                                        minLength: 2
                                        type: string
                                      maxNodeCount:
                                        default: 4
                                        description: |-
                                          MaxNodeCount caps the autoscale ceiling for this class. Defaults
                                          to 4 — small enough to prevent runaway spend, large enough for
                                          most pipelines.
                                        format: int32
                                        maximum: 64
                                        minimum: 1
                                        type: integer
                                      name:
                                        description: |-
                                          Name is the developer-facing class name (e.g. "cpu-balanced",
                                          "gpu-l4-small"). Becomes the NodePool name and the
                                          platform.kubecore.io/nodegroup-type label. Must be DNS-1035
                                          compatible (lowercase, digits, hyphens; starts with a letter,
                                          ends with a letter or digit).
                                        maxLength: 40
                                        minLength: 2
                                        pattern: ^[a-z][a-z0-9-]*[a-z0-9]$
                                        type: string
                                      preemptible:
                                        description: |-
                                          Preemptible enables spot-tier pricing (~70% discount, eviction
                                          risk). Only set true on classes that explicitly handle preemption
                                          in their workflow steps. Admission rejects preemptible:true unless
                                          ComputeProfile is "ephemeral-training" (#357/#361).
                                        type: boolean
                                      tier:
                                        description: |-
                                          Tier is "cpu" or "gpu". Drives default labels/taints and
                                          determines whether GuestAccelerator is required.
                                        enum:
                                          - cpu
                                          - gpu
                                        type: string
                                    required:
                                      - machineType
                                      - name
                                      - tier
                                    type: object
                                    x-kubernetes-validations:
                                      - message: preemptible:true is only allowed when computeProfile is ephemeral-training (stateful-serving/stateful-database classes must run on-demand to avoid data loss on preemption)
                                        rule: '!has(self.preemptible) || !self.preemptible || self.computeProfile == ''ephemeral-training'''
                                  maxItems: 32
                                  type: array
                              type: object
                            kubernetesVersion:
                              default: "1.35"
                              description: KubernetesVersion specifies the GKE version.
                              type: string
                            labels:
                              additionalProperties:
                                type: string
                              description: Labels are additional GCP resource labels to apply to the GKE cluster.
                              type: object
                            private:
                              default: true
                              description: Private enables private cluster mode (nodes get only private IPs).
                              type: boolean
                            releaseChannel:
                              default: REGULAR
                              description: ReleaseChannel specifies the GKE release channel for managed version upgrades.
                              enum:
                                - RAPID
                                - REGULAR
                                - STABLE
                              type: string
                            system:
                              description: System defines configuration for the system node pool (kubecoresys).
                              properties:
                                machineType:
                                  default: e2-standard-4
                                  description: MachineType specifies the GCE machine type for system nodes.
                                  type: string
                              type: object
                            type:
                              description: Type specifies whether this is a shared or dedicated cluster.
                              enum:
                                - shared
                                - dedicated
                              type: string
                          required:
                            - kubernetesVersion
                            - type
                          type: object
                        region:
                          description: Region is the GCP region where the GKE cluster will be created (e.g., europe-west1).
                          minLength: 1
                          type: string
                        zone:
                          description: |-
                            Zone optionally pins the zone for a zonal (non-production) GKE cluster, e.g.
                            "us-central1-a". When empty, the operator derives region + "-c" by default.
                            Set this to escape a GCE_STOCKOUT (capacity shortage) in the default "-c"
                            zone without changing the region. Must belong to spec Region. Ignored for
                            production (regional) clusters, which span all zones in the region.
                          pattern: ^[a-z]+-[a-z0-9]+-[a-z]$
                          type: string
                      required:
                        - cluster
                        - region
                      type: object
                  type: object
                clusterMode:
                  default: dev
                  description: |-
                    ClusterMode declares the intent of this cluster: "dev" (cost-optimized,
                    single-pool co-location, platform tools at replicas=1, zonal GKE placement)
                    or "production" (dedicated node pools, platform tools at replicas>=2 with
                    hostname anti-affinity, regional GKE placement).

                    Immutable after creation — switching modes would require destructive
                    rebuilds of node pools and workloads. To move a workload between modes,
                    create a new KubePool and migrate.

                    Default "dev" preserves backward compatibility with existing KubePools
                    that were created before this field existed and already run the
                    single-pool topology that dev mode codifies.
                  enum:
                    - dev
                    - production
                  type: string
                  x-kubernetes-validations:
                    - message: clusterMode is immutable
                      rule: self == oldSelf
                defaultRegistry:
                  description: |-
                    DefaultRegistry names the entry projects get when they declare no
                    spec.registry. Must name a declared entry; enforced by Kyverno (Task 9).
                  type: string
                features:
                  description: Features defines which platform features are enabled for this cluster
                  properties:
                    autoscalerHealer:
                      description: |-
                        AutoscalerHealer enables the autoscaler-backoff healer CronWorkflow, which
                        clears a stale GKE cluster-autoscaler backoff on scale-to-zero node pools
                        (e.g. kubecorecpu) so a transient scale-up failure doesn't leave pods
                        Pending for the full ~30min backoff window. Platform-wide (GCP): applies
                        to any min=0 pool. Defaults to true when not specified.
                      type: boolean
                    costTracking:
                      default: false
                      description: |-
                        CostTracking deploys OpenCost bound to the pool VictoriaMetrics so per-namespace
                        cost is computed and visible locally. Requires observability. Default false.
                      type: boolean
                    kyverno:
                      description: |-
                        Kyverno enables the Kyverno policy engine on this child cluster.
                        Defaults to true when not specified. Set to false to disable.
                      type: boolean
                    logging:
                      description: |-
                        Logging deploys VictoriaLogs plus the bundled Vector DaemonSet to aggregate
                        container logs pool-wide. Requires observability (Grafana renders the logs
                        datasource). Defaults to true when not specified.
                      type: boolean
                    mesh:
                      default: false
                      description: |-
                        Mesh enables the Istio ambient mesh on this child cluster: istio-base,
                        istiod (ambient profile, CA delegated to cert-manager-istio-csr),
                        istio-cni, ztunnel, and a per-pool two-tier cert-manager PKI.
                        Defaults to false. v1 installs the substrate only (PERMISSIVE mTLS);
                        namespaces are not enrolled until they opt in (PRD-ARCH-001).
                      type: boolean
                    ml:
                      description: |-
                        ML enables the ML platform tools (MLflow, lakeFS, pricing cache).
                        When true, creates an XMLSystem claim; requires Operators.Postgres when using shared Postgres.
                      type: boolean
                    mlConfig:
                      description: |-
                        MLConfig fine-tunes which ML components are provisioned when ML is true.
                        When nil, all default to true (backward compatible). Set to false to skip provisioning a component.
                      properties:
                        hpc:
                          description: |-
                            HPC is the LEGACY pool-level MeluXina switch (PRD-585). DEPRECATED by
                            PRD-HPC-1231 (tenancy is per project, KubeProject.spec.ml.hpc) and read
                            only by the migration bridge (D-09): while it is true and a project on
                            this pool has NOT declared spec.ml.hpc, that project keeps the pool-keyed
                            JWT and the static class list it had before the upgrade. Removed in the
                            train after every project on the pool is bound.

                            Deprecated: bind projects with KubeProject.spec.ml.hpc instead.
                          properties:
                            enabled:
                              description: |-
                                Enabled keeps the legacy pool-keyed MeluXina path alive for unbound
                                projects during the PRD-HPC-1231 migration.
                              type: boolean
                            provider:
                              description: Provider is ignored (kept so existing manifests validate).
                              enum:
                                - meluxina
                              type: string
                          type: object
                        mlflow:
                          description: Mlflow deploys MLflow (Postgres backend, optional S3 artifacts). Default true.
                          type: boolean
                        s3:
                          description: |-
                            S3 creates an S3 bucket and configures MLflow to use it for artifacts. Default true.
                            When false, MLflow uses the default artifact store (no S3 bucket or IRSA).
                          type: boolean
                      type: object
                    observability:
                      description: Observability enables VictoriaMetrics observability stack
                      type: boolean
                    tracing:
                      description: |-
                        Tracing deploys VictoriaTraces with an OTLP gRPC receiver, and wires Envoy
                        Gateway (north-south) and — when features.mesh is on — the Istio mesh
                        (east-west) to export spans to it. Requires observability (Grafana renders
                        the traces datasource). Defaults to true when not specified.
                      type: boolean
                    zotRegistry:
                      description: |-
                        ZotRegistry enables in-cluster Zot OCI container registry.
                        Defaults to true when not specified. Set to false to disable.
                      type: boolean
                  type: object
                  x-kubernetes-validations:
                    - message: features.costTracking requires features.observability (OpenCost reads the pool VictoriaMetrics)
                      rule: '!(has(self.costTracking) && self.costTracking) || (has(self.observability) && self.observability)'
                    - message: features.logging requires features.observability (Grafana renders the VictoriaLogs datasource)
                      rule: '!(has(self.logging) && self.logging) || !has(self.observability) || self.observability'
                    - message: features.tracing requires features.observability (Grafana renders the VictoriaTraces datasource)
                      rule: '!(has(self.tracing) && self.tracing) || !has(self.observability) || self.observability'
                kaosCloud:
                  description: KaosCloud configures KubeCore-managed cloud connectivity features.
                  properties:
                    costExport:
                      default: false
                      description: |-
                        CostExport pushes locally-computed cost metrics (OpenCost + vmagent) up to the control
                        plane over the interconnect. Requires features.costTracking and secureConnection.
                        Defaults to false: client data push is opt-in.
                      type: boolean
                    osr:
                      default: {}
                      description: |-
                        Osr configures the OSR edge stack on this pool: knowledge observation plus the
                        read-only live-state API. Requires secureConnection — both flows ride the interconnect.
                        The object-level default below is load-bearing, not decorative: CRD structural-schema
                        defaulting only fills in a field's default once its *parent* is present in the stored
                        object, so an omitted osr block would otherwise never materialize osr.enabled=true and
                        the CEL rule's has(self.osr) check below would never see it — silently admitting
                        secureConnection=false with OSR at its (Go-level) enabled default. The empty-object
                        default makes "osr omitted" and "osr: {}" the same stored document, closing that gap.
                      properties:
                        chartVersion:
                          description: |-
                            ChartVersion pins the OSR edge chart for this pool, overriding the fleet
                            default. Empty means follow the fleet. This is the canary and rollback control.
                          type: string
                        enabled:
                          default: true
                          description: Enabled deploys the OSR edge stack. Defaults to true when not specified.
                          type: boolean
                        liveQuery:
                          default: true
                          description: |-
                            LiveQuery deploys the read-only live-state API and its southbound exposure.
                            Separate from Enabled because a client may permit outbound observation while
                            refusing an inbound read surface. Defaults to true.
                          type: boolean
                      type: object
                    secureConnection:
                      default: true
                      description: |-
                        SecureConnection enables the secure control-plane↔pool interconnect (Skupper edge).
                        Defaults to true when not specified. Set to false to disable.
                      type: boolean
                  type: object
                  x-kubernetes-validations:
                    - message: kaosCloud.costExport requires kaosCloud.secureConnection (the cost push rides the interconnect)
                      rule: '!(has(self.costExport) && self.costExport) || (!has(self.secureConnection) || self.secureConnection)'
                    - message: kaosCloud.osr requires kaosCloud.secureConnection (both OSR data flows ride the interconnect)
                      rule: '!(has(self.osr) && has(self.osr.enabled) && self.osr.enabled) || (!has(self.secureConnection) || self.secureConnection)'
                kubeOrgRef:
                  description: KubeOrgRef references the KubeOrg that provides AWS configuration and network
                  minLength: 1
                  type: string
                observability:
                  description: Observability configures the pool's observability stack.
                  properties:
                    resourceProfile:
                      description: |-
                        ResourceProfile selects the VMCluster sizing tier for vmstorage,
                        vmselect and vminsert. Tiers are monotonic — small <= medium <= large on
                        every component, enforced by
                        compositions/apis/kubepool/observability/tests/test-resource-profile-ladder.sh.

                        Defaults to "small" when unset, which is the sizing the entire fleet
                        runs today. The default lives in the Go accessor
                        (KubePoolResource.ObservabilityResourceProfile), NOT via a
                        `+kubebuilder:default` marker on this field: CRD structural defaulting
                        only fills a child field once its PARENT is present in the stored
                        object, so defaulting this field would additionally require an
                        object-level default on Observability above — the same
                        load-bearing-default trap documented on KaosCloudConfig.Osr. Keeping
                        the default in Go also avoids rewriting every stored KubePool on
                        upgrade.
                      enum:
                        - small
                        - medium
                        - large
                      type: string
                  type: object
                operators:
                  description: |-
                    Operators enables cluster-scoped operators (e.g. CloudNativePG). When set, the
                    XClusterOperators composition installs the operator and a shared instance (e.g. one
                    Postgres cluster) so XMLSystem and other workloads can use it. Operators run before
                    XMLSystem; observability stays in XKubeSystem.
                  properties:
                    postgres:
                      description: |-
                        Postgres enables the CloudNativePG operator and a shared Postgres cluster (30Gi, 1 instance).
                        When true, XClusterOperators installs the operator and one Cluster; XMLSystem and other
                        workloads use this cluster instead of deploying their own Postgres.
                      type: boolean
                    redis:
                      description: |-
                        Redis enables a shared Redis instance (PRD-ARCH-002 F-16 — the raw-workload
                        ToolBinding archetype, proven alongside postgres-cnpg's operator-CR archetype).
                        Unlike Postgres (a separate CloudNativePG operator installing a Cluster CR),
                        Redis has no operator: this flag directly gates a single Helm Release that IS
                        the instance. See XClusterOperators' spec.provisionSharedRedis (definition.yaml)
                        for the second, instance-level gate — mirroring spec.provisionSharedPostgres.
                      type: boolean
                  type: object
                registries:
                  description: |-
                    Registries declares the container registries workloads on this pool may
                    use. When empty, the operator synthesises a single "default" entry of
                    type zot iff features.zotRegistry resolves true — so existing pools
                    behave identically without declaring anything (R-11).
                  items:
                    description: |-
                      RegistrySpec declares one container registry that workloads on this pool may
                      push to and pull from. Projects select an entry by Name
                      (KubeProject.spec.registry); names bind late, per pool, in the manner of
                      storageClassName — the same name may resolve to GAR on a GCP pool and ECR on
                      an AWS pool.
                    properties:
                      gar:
                        description: |-
                          GAR is required when Type is "gar" and ignored otherwise. This
                          invariant is not currently enforced at admission — see
                          PRD-REG-994 and internal/policy/seed.go, where it is documented as
                          one of three cross-field, same-object invariants the policy
                          catalog's closed Rule set cannot express. Phase 2 must add
                          enforcement alongside the "gar" kind-table row.
                        properties:
                          location:
                            description: Location is the GAR location, e.g. "europe-west1".
                            minLength: 1
                            type: string
                        required:
                          - location
                        type: object
                      name:
                        description: |-
                          Name is the selector projects use. Unique within the pool, enforced by
                          the listType=map marker on KubePoolSpec.Registries rather than by CEL.
                        maxLength: 63
                        minLength: 1
                        pattern: ^[a-z0-9][a-z0-9-]*[a-z0-9]$
                        type: string
                      type:
                        description: Type selects the kind-table row.
                        enum:
                          - zot
                          - gar
                          - ecr
                          - acr
                        type: string
                    required:
                      - name
                      - type
                    type: object
                  type: array
                  x-kubernetes-list-map-keys:
                    - name
                  x-kubernetes-list-type: map
                systemProfile:
                  description: |-
                    SystemProfile controls the sizing of the platform system node group.
                    The composition set translates this to provider-specific instance types and scaling.
                    "small": minimal footprint (dev/test), "medium": balanced (staging),
                    "ha": high availability (production).
                  enum:
                    - small
                    - medium
                    - ha
                  type: string
                tls:
                  description: TLS defines TLS configuration for the cluster platform system
                  properties:
                    issuer:
                      description: Issuer defines ClusterIssuer configuration
                      properties:
                        acmeServer:
                          default: https://acme-v02.api.letsencrypt.org/directory
                          description: ACMEServer URL for certificate authority
                          type: string
                        email:
                          default: kaos@kubecore.io
                          description: Email for ACME account registration
                          type: string
                        enabled:
                          default: true
                          description: Enabled determines if automatic ClusterIssuer should be created
                          type: boolean
                      required:
                        - enabled
                      type: object
                  type: object
              required:
                - cloudProvider
                - kubeOrgRef
              type: object
              x-kubernetes-validations:
                - message: exactly one cloud provider must be specified
                  rule: '(has(self.cloudProvider.aws) ? 1 : 0) + (has(self.cloudProvider.gcp) ? 1 : 0) + (has(self.cloudProvider.azure) ? 1 : 0) == 1'
                - message: kaosCloud.costExport requires features.costTracking (you can only push cost you compute)
                  rule: '!(has(self.kaosCloud) && has(self.kaosCloud.costExport) && self.kaosCloud.costExport) || (has(self.features) && has(self.features.costTracking) && self.features.costTracking)'
            status:
              description: KubePoolStatus defines the observed state of KubePool
              properties:
                argoWorkflowsOIDC:
                  description: ArgoWorkflowsOIDC contains the Zitadel OIDC app status for Argo Workflows SSO
                  properties:
                    appId:
                      description: AppID is the Zitadel application ID
                      type: string
                    clientId:
                      description: ClientID is the OIDC client ID
                      type: string
                    projectId:
                      description: ProjectID is the Zitadel project the app belongs to
                      type: string
                    ready:
                      description: Ready indicates the OIDC app is created and credentials are pushed to AWS SM
                      type: boolean
                    secretName:
                      description: SecretName is the AWS Secrets Manager key name for OIDC credentials
                      type: string
                  type: object
                argocdOIDC:
                  description: ArgocdOIDC contains the Zitadel OIDC app status for ArgoCD SSO
                  properties:
                    appId:
                      description: AppID is the Zitadel application ID
                      type: string
                    clientId:
                      description: ClientID is the OIDC client ID
                      type: string
                    projectId:
                      description: ProjectID is the Zitadel project the app belongs to
                      type: string
                    ready:
                      description: Ready indicates the OIDC app is created and credentials are pushed to AWS SM
                      type: boolean
                    secretName:
                      description: SecretName is the AWS Secrets Manager key name for OIDC credentials
                      type: string
                  type: object
                cloudProvider:
                  description: |-
                    CloudProvider records the active cloud provider name (e.g., "aws") at the time
                    the Reconciling phase completes. Used by the DeletingPhase to route to the correct
                    CompositionSetContract even when spec.cloudProvider may have been cleared.
                    RETAINED (not moved to Outputs) despite being a §3.2 MOVE candidate: the
                    original plan (r1 §3.2) already ratified this as surviving deletion because
                    the pool's own DeletingPhase reads it after spec.cloudProvider may be
                    cleared — same-CR, own-DeletingPhase read. Track 1e additionally found two
                    cross-CR readers with documented deliberate-non-migration rationale
                    (kubeapp/phases/dedicated_environment_xkubenv.go resolveDedicatedEnvCloudKey;
                    kubeproject/phases/reconciling_ml.go resolveCloudKey) that predate this MOVE
                    pass — both already carry their own non-migration comments, not introduced
                    here. Outputs.CloudProvider is still filled from this field (Reporting).
                  type: string
                cluster:
                  description: Cluster contains a reference to the created cluster XR resource
                  properties:
                    apiVersion:
                      description: API version of the resource
                      type: string
                    kind:
                      description: Kind of the resource
                      type: string
                    name:
                      description: Name of the resource
                      type: string
                    namespace:
                      description: Namespace of the resource (if namespaced)
                      type: string
                    uid:
                      description: UID of the resource
                      type: string
                  type: object
                clusterAddons:
                  description: ClusterAddons contains status of managed cluster add-ons
                  items:
                    description: ClusterAddonStatus contains the status of a managed cluster add-on
                    properties:
                      name:
                        description: Name of the add-on
                        type: string
                      status:
                        description: Status of the add-on
                        type: string
                      version:
                        description: Version of the add-on
                        type: string
                    type: object
                  type: array
                clusterEndpoint:
                  description: ClusterEndpoint contains the endpoint URL of the EKS cluster
                  type: string
                clusterInfo:
                  description: ClusterInfo contains detailed cluster identity (name, ARN, actual version, region)
                  properties:
                    arn:
                      description: |-
                        ARN is the cloud-provider resource identifier for the cluster.
                        For AWS EKS this is the cluster ARN. Not set for GKE.
                      type: string
                    clusterName:
                      description: ClusterName is the cluster name as known to the cloud provider
                      type: string
                    region:
                      description: Region is the cloud region where the cluster is deployed
                      type: string
                    version:
                      description: Version is the actual running Kubernetes version
                      type: string
                  type: object
                clusterOIDC:
                  description: ClusterOIDC contains OIDC provider details for IRSA configuration
                  properties:
                    clientId:
                      description: ClientID is the OIDC client ID
                      type: string
                    issuerUrl:
                      description: IssuerURL is the OIDC issuer URL
                      type: string
                    providerArn:
                      description: ProviderARN is the OIDC provider ARN
                      type: string
                    providerName:
                      description: ProviderName is the OIDC provider resource name
                      type: string
                  type: object
                clusterStatus:
                  description: ClusterStatus contains the current status of the EKS cluster
                  type: string
                clusterType:
                  description: ClusterType is the cluster type (shared or dedicated)
                  type: string
                conditions:
                  description: Conditions represent the latest available observations of the KubePool's current state
                  items:
                    description: Condition contains details for one aspect of the current state of this API Resource.
                    properties:
                      lastTransitionTime:
                        description: |-
                          lastTransitionTime is the last time the condition transitioned from one status to another.
                          This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                        format: date-time
                        type: string
                      message:
                        description: |-
                          message is a human readable message indicating details about the transition.
                          This may be an empty string.
                        maxLength: 32768
                        type: string
                      observedGeneration:
                        description: |-
                          observedGeneration represents the .metadata.generation that the condition was set based upon.
                          For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                          with respect to the current state of the instance.
                        format: int64
                        minimum: 0
                        type: integer
                      reason:
                        description: |-
                          reason contains a programmatic identifier indicating the reason for the condition's last transition.
                          Producers of specific condition types may define expected values and meanings for this field,
                          and whether the values are considered a guaranteed API.
                          The value should be a CamelCase string.
                          This field may not be empty.
                        maxLength: 1024
                        minLength: 1
                        pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                        type: string
                      status:
                        description: status of the condition, one of True, False, Unknown.
                        enum:
                          - "True"
                          - "False"
                          - Unknown
                        type: string
                      type:
                        description: type of condition in CamelCase or in foo.example.com/CamelCase.
                        maxLength: 316
                        pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                        type: string
                    required:
                      - lastTransitionTime
                      - message
                      - reason
                      - status
                      - type
                    type: object
                  type: array
                  x-kubernetes-list-map-keys:
                    - type
                  x-kubernetes-list-type: map
                costExport:
                  description: CostExport contains the cost-export (push) pipeline status when enabled.
                  properties:
                    message:
                      description: Message carries detail when not Ready.
                      type: string
                    ready:
                      description: Ready indicates the cost-export (push) pipeline is Ready.
                      type: boolean
                    state:
                      description: 'State is a rollup: Disabled | Blocked | Provisioning | Ready.'
                      type: string
                  type: object
                costTracking:
                  description: CostTracking contains the local cost-tracking (OpenCost) status when enabled.
                  properties:
                    message:
                      description: Message carries detail when not Ready.
                      type: string
                    ready:
                      description: Ready indicates the local cost-tracking pipeline (OpenCost) is Ready.
                      type: boolean
                    state:
                      description: 'State is a rollup: Disabled | Blocked | Provisioning | Ready.'
                      type: string
                  type: object
                deletionRetryCount:
                  description: DeletionRetryCount tracks retry attempts during deletion phase for timeout/backoff logic
                  type: integer
                deploymentTiming:
                  description: DeploymentTiming contains system deployment lifecycle timestamps
                  properties:
                    lastUpdate:
                      description: LastUpdate is the last status update time
                      type: string
                    startTime:
                      description: StartTime is when deployment started
                      type: string
                  type: object
                derivedStatus:
                  description: |-
                    DerivedStatus reports the freshness of this resource's derived status,
                    maintained by the framework's Ready prologue.
                  properties:
                    lastVerifiedTime:
                      description: |-
                        LastVerifiedTime is when every registered ReadyUnit last evaluated
                        successfully. It does NOT advance on a pass where any unit failed.
                      format: date-time
                      type: string
                    staleFields:
                      description: |-
                        StaleFields lists the status fields not refreshed on the last pass
                        because their owning unit failed. Sorted for stable diffs.
                      items:
                        type: string
                      type: array
                    unitsStale:
                      description: UnitsStale counts units whose evaluation failed on the last pass.
                      type: integer
                    unitsVerified:
                      description: UnitsVerified counts units that evaluated successfully on the last pass.
                      type: integer
                  type: object
                gatewayEndpoint:
                  description: |-
                    GatewayEndpoint is the reserved ingress LoadBalancer IP for the pool's
                    Envoy Gateway (always reserved; consumed by EnvoyProxy.loadBalancerIP).
                  type: string
                grafanaOIDC:
                  description: GrafanaOIDC contains the Zitadel OIDC app status for Grafana SSO
                  properties:
                    appId:
                      description: AppID is the Zitadel application ID
                      type: string
                    clientId:
                      description: ClientID is the OIDC client ID
                      type: string
                    projectId:
                      description: ProjectID is the Zitadel project the app belongs to
                      type: string
                    ready:
                      description: Ready indicates the OIDC app is created and credentials are pushed to AWS SM
                      type: boolean
                    secretName:
                      description: SecretName is the AWS Secrets Manager key name for OIDC credentials
                      type: string
                  type: object
                kubernetesVersion:
                  description: KubernetesVersion is the requested Kubernetes version for this cluster
                  type: string
                lastTransitionTime:
                  description: LastTransitionTime records when the status was last updated
                  format: date-time
                  type: string
                networkConfig:
                  description: |-
                    NetworkConfig contains the network configuration allocated for this cluster.
                    RETAINED (not moved to Outputs) despite being a §3.2 MOVE candidate: it is
                    an own-CR cross-phase bootstrap field — the Syncing phase (network/cidr.go,
                    phases/syncing_params.go) reads back THIS field's prior-reconcile value to
                    plan idempotent CIDR/subnet allocation on subsequent passes, well before
                    the Reporting phase (the sole producer of Outputs) ever runs on that pass.
                    Outputs.Network is filled from this field once Reporting runs (see
                    phases/reporting.go), so the contract copy still exists; only the own-CR
                    bootstrap copy survives here. Track 1e MOVE-pass deviation.
                  properties:
                    allocatedFrom:
                      description: AllocatedFrom shows the source VPC CIDR this allocation was derived from
                      type: string
                    availabilityZones:
                      description: AvailabilityZones lists the AZs where subnets are created
                      items:
                        type: string
                      type: array
                    natGateways:
                      additionalProperties:
                        type: string
                      description: NATGateways maps availability zones to NAT Gateway IDs for private subnet routing
                      type: object
                    subnetCIDRs:
                      description: SubnetCIDRs are the CIDR blocks allocated for the cluster subnets
                      items:
                        type: string
                      type: array
                    vpcId:
                      description: VPCID is the ID of the VPC where the cluster is deployed
                      type: string
                  type: object
                observability:
                  description: Observability contains structured observability stack status
                  properties:
                    components:
                      description: Components contains individual component statuses
                      properties:
                        grafana:
                          description: Grafana component status
                          properties:
                            ready:
                              description: Ready indicates if the component is ready
                              type: boolean
                            replicas:
                              description: Replicas is the number of replicas
                              type: integer
                          type: object
                        victoriaLogs:
                          description: VictoriaLogs component status (log aggregation backend)
                          properties:
                            ready:
                              description: Ready indicates if the component is ready
                              type: boolean
                            replicas:
                              description: Replicas is the number of replicas
                              type: integer
                          type: object
                        victoriaTraces:
                          description: VictoriaTraces component status (OTLP trace backend)
                          properties:
                            ready:
                              description: Ready indicates if the component is ready
                              type: boolean
                            replicas:
                              description: Replicas is the number of replicas
                              type: integer
                          type: object
                        vmagent:
                          description: VMAgent component status
                          properties:
                            ready:
                              description: Ready indicates if the component is ready
                              type: boolean
                            replicas:
                              description: Replicas is the number of replicas
                              type: integer
                          type: object
                        vmalert:
                          description: VMAlert component status
                          properties:
                            ready:
                              description: Ready indicates if the component is ready
                              type: boolean
                            replicas:
                              description: Replicas is the number of replicas
                              type: integer
                          type: object
                        vminsert:
                          description: VMInsert component status
                          properties:
                            ready:
                              description: Ready indicates if the component is ready
                              type: boolean
                            replicas:
                              description: Replicas is the number of replicas
                              type: integer
                          type: object
                        vmselect:
                          description: VMSelect component status
                          properties:
                            ready:
                              description: Ready indicates if the component is ready
                              type: boolean
                            replicas:
                              description: Replicas is the number of replicas
                              type: integer
                          type: object
                        vmstorage:
                          description: VMStorage component status
                          properties:
                            ready:
                              description: Ready indicates if the component is ready
                              type: boolean
                            replicas:
                              description: Replicas is the number of replicas
                              type: integer
                          type: object
                      type: object
                    grafanaUrl:
                      description: GrafanaURL is the Grafana access URL
                      type: string
                    installed:
                      description: Installed indicates if the observability stack is installed
                      type: boolean
                    metrics:
                      description: Metrics contains aggregated observability metrics
                      properties:
                        samplesPerSecond:
                          description: SamplesPerSecond is the ingestion rate (serialized as string)
                          type: string
                        storageUsageGB:
                          description: StorageUsageGB is the total storage usage in GB (serialized as string)
                          type: string
                        timeSeriesCount:
                          description: TimeSeriesCount is the total number of time series
                          type: integer
                      type: object
                    mode:
                      description: Mode is the deployment mode (single, ha)
                      type: string
                    namespace:
                      description: Namespace where observability components are installed
                      type: string
                    version:
                      description: Version is the installed VictoriaMetrics version
                      type: string
                  type: object
                observabilityStatus:
                  description: |-
                    ObservabilityStatus contains observability stack status extracted from the associated XKubeSystem.
                    This is schemaless and may evolve with the underlying composition status fields.
                  x-kubernetes-preserve-unknown-fields: true
                observedGeneration:
                  description: ObservedGeneration is the most recent generation observed by the controller
                  format: int64
                  type: integer
                osr:
                  description: |-
                    Osr reports the OSR edge operator's state on this pool. Reporting only — OSR is
                    deliberately NOT on the pool readiness critical path, so a broken edge install
                    degrades observability without holding the pool out of Ready.
                  properties:
                    chartVersion:
                      description: |-
                        ChartVersion is the RESOLVED kaos-osr-edge-operator chart version for this pool:
                        the per-pool pin if set, else the fleet default, else the compiled-in floor. It is
                        the resolved value rather than the spec override because a pool following the fleet
                        has an empty override, and answering "what version is this pool on" with an empty
                        string is exactly the gap this field closes.
                      type: string
                    message:
                      description: Message carries detail when not Ready (e.g. the unready composed resource).
                      type: string
                    ready:
                      description: Ready indicates the XKnowledge XR is Ready.
                      type: boolean
                    state:
                      description: 'State is a human-readable rollup: Pending | Ready | Degraded | Disabled.'
                      type: string
                  type: object
                outputs:
                  description: |-
                    Outputs is the deliberate cross-controller contract of this KubePool
                    (remediation plan r2 §2.2). Consumers access it ONLY through
                    parentref.GetKubePoolOutputs, which gates on Ready + observedGeneration.
                    Everything else in this status struct is observational and MUST NOT be
                    read by other controllers (enforced repo-wide by the crossstatusreads
                    analyzer).
                  properties:
                    baseDns:
                      type: string
                    capabilities:
                      additionalProperties:
                        type: boolean
                      description: |-
                        Capabilities reports which cluster-scoped tool operators have LANDED on
                        this pool (PRD-ARCH-002 F-13/D-21 capability gate), keyed by the same
                        string a ToolBinding recipe writes into spec.requiredOperator (e.g.
                        "cloudnative-pg" — see config/samples/
                        schema_v1beta1_toolbinding_postgres_cnpg.yaml), NOT by the
                        status.platformTools JSON key ("postgres"): the two vocabularies
                        differ, and kubepool/phases/reporting.go's buildCapabilities is the one
                        place that joins them. D-21: an operator is landed the moment it is
                        Ready, independent of whether any instance (e.g. a shared Postgres
                        Cluster) was ever provisioned — that is D-21's separate, second gate
                        and is deliberately not reflected here. Never required for Ready (an
                        absent or false entry just means the pool's spec never enabled that
                        operator); consumed by kubeapp/phases' F-13 CapabilityMissing check via
                        parentref.GetKubePoolOutputs, never a direct cross-CR status read.
                      type: object
                    cloudProvider:
                      description: Core — required for Ready; the helper rejects Ready outputs with any of these empty.
                      type: string
                    clusterName:
                      description: |-
                        ClusterName is DELIBERATELY duplicated in observational status.clusterInfo.clusterName.
                        Invariant: both copies are assigned from the same local variable in the same
                        Reporting write (single atomic status update); a unit test asserts equality.
                        Controllers may read only this copy (lint-enforced); clusterInfo is display-only.
                      type: string
                    gcpProjectId:
                      description: |-
                        GcpProjectID is the GCP project this pool's cloud resources live in,
                        derived from the parent KubeOrg's spec.gcpConfig.projectId (PRD-REG-994
                        R-02). Empty on non-GCP pools, and NOT required for Ready — a GAR
                        registry entry resolved against a pool with this empty fails closed in
                        registryref.endpointsFor rather than producing an image path with an
                        empty project segment.
                      type: string
                    kubernetesProviderConfig:
                      description: KubernetesProviderConfig is required; Ready outputs must have Name != "" && Ready.
                      properties:
                        name:
                          description: Name of the provider config
                          type: string
                        ready:
                          description: Ready indicates if the provider config is ready
                          type: boolean
                      type: object
                    network:
                      description: NetworkConfiguration contains network allocation information for the cluster
                      properties:
                        allocatedFrom:
                          description: AllocatedFrom shows the source VPC CIDR this allocation was derived from
                          type: string
                        availabilityZones:
                          description: AvailabilityZones lists the AZs where subnets are created
                          items:
                            type: string
                          type: array
                        natGateways:
                          additionalProperties:
                            type: string
                          description: NATGateways maps availability zones to NAT Gateway IDs for private subnet routing
                          type: object
                        subnetCIDRs:
                          description: SubnetCIDRs are the CIDR blocks allocated for the cluster subnets
                          items:
                            type: string
                          type: array
                        vpcId:
                          description: VPCID is the ID of the VPC where the cluster is deployed
                          type: string
                      type: object
                    observedGeneration:
                      description: |-
                        Stamped by the SAME producer write that fills the rest of this struct
                        (the Reporting-phase rendering pass), from resource.GetGeneration() read at
                        render time. NEVER defaulted or filled by status_updater (design decision 3).
                      format: int64
                      type: integer
                    region:
                      type: string
                    registryEndpoint:
                      description: |-
                        RegistryEndpoint is feature-conditional, NOT core: required for Ready only when
                        spec.features.zotRegistry is enabled (defaults to true). A pool with zotRegistry
                        explicitly set to false legitimately reaches Ready with this field empty —
                        parentref.GetKubePoolOutputs mirrors the same enable/default logic the operator's
                        own syncing phase uses before gating on it (plan r2 remediation Defect 2).
                      type: string
                    xKubeSystemRef:
                      description: ResourceReference contains a reference to a Kubernetes resource
                      properties:
                        apiVersion:
                          description: API version of the resource
                          type: string
                        kind:
                          description: Kind of the resource
                          type: string
                        name:
                          description: Name of the resource
                          type: string
                        namespace:
                          description: Namespace of the resource (if namespaced)
                          type: string
                        uid:
                          description: UID of the resource
                          type: string
                      type: object
                  type: object
                phase:
                  description: Phase indicates the current reconciliation phase
                  enum:
                    - Reconciling
                    - Syncing
                    - Reporting
                    - Ready
                    - Failed
                    - Deleting
                  type: string
                platformTools:
                  description: PlatformTools contains aggregated status of all platform tools with endpoints
                  properties:
                    argoRollouts:
                      description: ArgoRollouts tool status
                      properties:
                        enabled:
                          description: Enabled indicates if the tool is enabled
                          type: boolean
                        namespace:
                          description: Namespace where the tool is installed
                          type: string
                        status:
                          description: Status of the tool (Pending, Installing, Ready, Error)
                          type: string
                        trustDomain:
                          description: |-
                            TrustDomain is the SPIFFE trust domain reported by the tool. This field is
                            generic on the shared PlatformToolStatus struct but is only ever populated
                            for the mesh tool, where it carries the pool-scoped trust domain
                            ({pool}.{org}.mesh.kaos-labs.org) read back from the XNetworkMesh XR's
                            status — never recomputed by the operator (DEC-C1).
                          type: string
                        url:
                          description: URL is the access URL for the tool (if applicable)
                          type: string
                        version:
                          description: Version of the tool
                          type: string
                      type: object
                    argoWorkflows:
                      description: ArgoWorkflows tool status
                      properties:
                        enabled:
                          description: Enabled indicates if the tool is enabled
                          type: boolean
                        namespace:
                          description: Namespace where the tool is installed
                          type: string
                        status:
                          description: Status of the tool (Pending, Installing, Ready, Error)
                          type: string
                        trustDomain:
                          description: |-
                            TrustDomain is the SPIFFE trust domain reported by the tool. This field is
                            generic on the shared PlatformToolStatus struct but is only ever populated
                            for the mesh tool, where it carries the pool-scoped trust domain
                            ({pool}.{org}.mesh.kaos-labs.org) read back from the XNetworkMesh XR's
                            status — never recomputed by the operator (DEC-C1).
                          type: string
                        url:
                          description: URL is the access URL for the tool (if applicable)
                          type: string
                        version:
                          description: Version of the tool
                          type: string
                      type: object
                    argocd:
                      description: ArgoCD tool status
                      properties:
                        enabled:
                          description: Enabled indicates if the tool is enabled
                          type: boolean
                        namespace:
                          description: Namespace where the tool is installed
                          type: string
                        status:
                          description: Status of the tool (Pending, Installing, Ready, Error)
                          type: string
                        trustDomain:
                          description: |-
                            TrustDomain is the SPIFFE trust domain reported by the tool. This field is
                            generic on the shared PlatformToolStatus struct but is only ever populated
                            for the mesh tool, where it carries the pool-scoped trust domain
                            ({pool}.{org}.mesh.kaos-labs.org) read back from the XNetworkMesh XR's
                            status — never recomputed by the operator (DEC-C1).
                          type: string
                        url:
                          description: URL is the access URL for the tool (if applicable)
                          type: string
                        version:
                          description: Version of the tool
                          type: string
                      type: object
                    certManager:
                      description: CertManager tool status
                      properties:
                        enabled:
                          description: Enabled indicates if the tool is enabled
                          type: boolean
                        namespace:
                          description: Namespace where the tool is installed
                          type: string
                        status:
                          description: Status of the tool (Pending, Installing, Ready, Error)
                          type: string
                        trustDomain:
                          description: |-
                            TrustDomain is the SPIFFE trust domain reported by the tool. This field is
                            generic on the shared PlatformToolStatus struct but is only ever populated
                            for the mesh tool, where it carries the pool-scoped trust domain
                            ({pool}.{org}.mesh.kaos-labs.org) read back from the XNetworkMesh XR's
                            status — never recomputed by the operator (DEC-C1).
                          type: string
                        url:
                          description: URL is the access URL for the tool (if applicable)
                          type: string
                        version:
                          description: Version of the tool
                          type: string
                      type: object
                    crossplane:
                      description: Crossplane tool status
                      properties:
                        enabled:
                          description: Enabled indicates if the tool is enabled
                          type: boolean
                        namespace:
                          description: Namespace where the tool is installed
                          type: string
                        status:
                          description: Status of the tool (Pending, Installing, Ready, Error)
                          type: string
                        trustDomain:
                          description: |-
                            TrustDomain is the SPIFFE trust domain reported by the tool. This field is
                            generic on the shared PlatformToolStatus struct but is only ever populated
                            for the mesh tool, where it carries the pool-scoped trust domain
                            ({pool}.{org}.mesh.kaos-labs.org) read back from the XNetworkMesh XR's
                            status — never recomputed by the operator (DEC-C1).
                          type: string
                        url:
                          description: URL is the access URL for the tool (if applicable)
                          type: string
                        version:
                          description: Version of the tool
                          type: string
                      type: object
                    externalSecrets:
                      description: ExternalSecrets tool status
                      properties:
                        enabled:
                          description: Enabled indicates if the tool is enabled
                          type: boolean
                        namespace:
                          description: Namespace where the tool is installed
                          type: string
                        status:
                          description: Status of the tool (Pending, Installing, Ready, Error)
                          type: string
                        trustDomain:
                          description: |-
                            TrustDomain is the SPIFFE trust domain reported by the tool. This field is
                            generic on the shared PlatformToolStatus struct but is only ever populated
                            for the mesh tool, where it carries the pool-scoped trust domain
                            ({pool}.{org}.mesh.kaos-labs.org) read back from the XNetworkMesh XR's
                            status — never recomputed by the operator (DEC-C1).
                          type: string
                        url:
                          description: URL is the access URL for the tool (if applicable)
                          type: string
                        version:
                          description: Version of the tool
                          type: string
                      type: object
                    gitopsPromoter:
                      description: GitOpsPromoter tool status
                      properties:
                        enabled:
                          description: Enabled indicates if the tool is enabled
                          type: boolean
                        namespace:
                          description: Namespace where the tool is installed
                          type: string
                        status:
                          description: Status of the tool (Pending, Installing, Ready, Error)
                          type: string
                        trustDomain:
                          description: |-
                            TrustDomain is the SPIFFE trust domain reported by the tool. This field is
                            generic on the shared PlatformToolStatus struct but is only ever populated
                            for the mesh tool, where it carries the pool-scoped trust domain
                            ({pool}.{org}.mesh.kaos-labs.org) read back from the XNetworkMesh XR's
                            status — never recomputed by the operator (DEC-C1).
                          type: string
                        url:
                          description: URL is the access URL for the tool (if applicable)
                          type: string
                        version:
                          description: Version of the tool
                          type: string
                      type: object
                    grafana:
                      description: |-
                        Grafana tool status (sourced from XObservability — synthesized from
                        status.observability.components.grafana and top-level namespace/version/installed fields)
                      properties:
                        enabled:
                          description: Enabled indicates if the tool is enabled
                          type: boolean
                        namespace:
                          description: Namespace where the tool is installed
                          type: string
                        status:
                          description: Status of the tool (Pending, Installing, Ready, Error)
                          type: string
                        trustDomain:
                          description: |-
                            TrustDomain is the SPIFFE trust domain reported by the tool. This field is
                            generic on the shared PlatformToolStatus struct but is only ever populated
                            for the mesh tool, where it carries the pool-scoped trust domain
                            ({pool}.{org}.mesh.kaos-labs.org) read back from the XNetworkMesh XR's
                            status — never recomputed by the operator (DEC-C1).
                          type: string
                        url:
                          description: URL is the access URL for the tool (if applicable)
                          type: string
                        version:
                          description: Version of the tool
                          type: string
                      type: object
                    kyverno:
                      description: |-
                        Kyverno tool status (sourced from XClusterOperators status.tools.kyverno; nil when the
                        features.kyverno flag is off or the composition has not yet written the entry).
                      properties:
                        enabled:
                          description: Enabled indicates if the tool is enabled
                          type: boolean
                        namespace:
                          description: Namespace where the tool is installed
                          type: string
                        status:
                          description: Status of the tool (Pending, Installing, Ready, Error)
                          type: string
                        trustDomain:
                          description: |-
                            TrustDomain is the SPIFFE trust domain reported by the tool. This field is
                            generic on the shared PlatformToolStatus struct but is only ever populated
                            for the mesh tool, where it carries the pool-scoped trust domain
                            ({pool}.{org}.mesh.kaos-labs.org) read back from the XNetworkMesh XR's
                            status — never recomputed by the operator (DEC-C1).
                          type: string
                        url:
                          description: URL is the access URL for the tool (if applicable)
                          type: string
                        version:
                          description: Version of the tool
                          type: string
                      type: object
                    mesh:
                      description: |-
                        Mesh reports the Istio ambient mesh installation, extracted from
                        XClusterOperators status.tools.mesh (worst-state across the five
                        mesh Helm releases).
                      properties:
                        enabled:
                          description: Enabled indicates if the tool is enabled
                          type: boolean
                        namespace:
                          description: Namespace where the tool is installed
                          type: string
                        status:
                          description: Status of the tool (Pending, Installing, Ready, Error)
                          type: string
                        trustDomain:
                          description: |-
                            TrustDomain is the SPIFFE trust domain reported by the tool. This field is
                            generic on the shared PlatformToolStatus struct but is only ever populated
                            for the mesh tool, where it carries the pool-scoped trust domain
                            ({pool}.{org}.mesh.kaos-labs.org) read back from the XNetworkMesh XR's
                            status — never recomputed by the operator (DEC-C1).
                          type: string
                        url:
                          description: URL is the access URL for the tool (if applicable)
                          type: string
                        version:
                          description: Version of the tool
                          type: string
                      type: object
                    postgres:
                      description: |-
                        Postgres reports the CloudNativePG OPERATOR installed by this pool,
                        sourced from XClusterOperators status.tools.postgres; nil when
                        spec.operators.postgres is off (D-21 gate 1) or the composition has not
                        yet written the entry. It reports the operator, NOT the shared Postgres
                        Cluster instance — spec.provisionSharedPostgres gates that separately.
                        Version is the pool's authoritative CNPG chart version (PRD-ARCH-002
                        D-03): the KubePool installs the operator, so the KubePool reports it.
                      properties:
                        enabled:
                          description: Enabled indicates if the tool is enabled
                          type: boolean
                        namespace:
                          description: Namespace where the tool is installed
                          type: string
                        status:
                          description: Status of the tool (Pending, Installing, Ready, Error)
                          type: string
                        trustDomain:
                          description: |-
                            TrustDomain is the SPIFFE trust domain reported by the tool. This field is
                            generic on the shared PlatformToolStatus struct but is only ever populated
                            for the mesh tool, where it carries the pool-scoped trust domain
                            ({pool}.{org}.mesh.kaos-labs.org) read back from the XNetworkMesh XR's
                            status — never recomputed by the operator (DEC-C1).
                          type: string
                        url:
                          description: URL is the access URL for the tool (if applicable)
                          type: string
                        version:
                          description: Version of the tool
                          type: string
                      type: object
                    redis:
                      description: |-
                        Redis reports the shared Redis Helm release installed by this pool
                        (PRD-ARCH-002 F-16 raw-workload archetype), sourced from XClusterOperators
                        status.tools.redis; nil when spec.operators.redis is off (D-21 gate 1) or
                        the composition has not yet written the entry. Unlike Postgres (a separate
                        CloudNativePG operator fronting a Cluster instance), Redis has no operator
                        distinct from the instance — this Status entry only reports Ready once the
                        shared Release itself is deployed, i.e. once BOTH D-21 gates
                        (spec.operators.redis AND spec.provisionSharedRedis) are satisfied. That is
                        the raw-workload archetype's proof point: the capability IS the instance.
                      properties:
                        enabled:
                          description: Enabled indicates if the tool is enabled
                          type: boolean
                        namespace:
                          description: Namespace where the tool is installed
                          type: string
                        status:
                          description: Status of the tool (Pending, Installing, Ready, Error)
                          type: string
                        trustDomain:
                          description: |-
                            TrustDomain is the SPIFFE trust domain reported by the tool. This field is
                            generic on the shared PlatformToolStatus struct but is only ever populated
                            for the mesh tool, where it carries the pool-scoped trust domain
                            ({pool}.{org}.mesh.kaos-labs.org) read back from the XNetworkMesh XR's
                            status — never recomputed by the operator (DEC-C1).
                          type: string
                        url:
                          description: URL is the access URL for the tool (if applicable)
                          type: string
                        version:
                          description: Version of the tool
                          type: string
                      type: object
                    skupper:
                      description: |-
                        Skupper (secure interconnect) tool status. Synthesized from XInterconnect status when
                        spec.kaosCloud.secureConnection is enabled; nil when the feature is disabled.
                      properties:
                        enabled:
                          description: Enabled indicates if the tool is enabled
                          type: boolean
                        namespace:
                          description: Namespace where the tool is installed
                          type: string
                        status:
                          description: Status of the tool (Pending, Installing, Ready, Error)
                          type: string
                        trustDomain:
                          description: |-
                            TrustDomain is the SPIFFE trust domain reported by the tool. This field is
                            generic on the shared PlatformToolStatus struct but is only ever populated
                            for the mesh tool, where it carries the pool-scoped trust domain
                            ({pool}.{org}.mesh.kaos-labs.org) read back from the XNetworkMesh XR's
                            status — never recomputed by the operator (DEC-C1).
                          type: string
                        url:
                          description: URL is the access URL for the tool (if applicable)
                          type: string
                        version:
                          description: Version of the tool
                          type: string
                      type: object
                    systemStatus:
                      description: SystemStatus is the overall system deployment status (Pending/Deploying/Ready/Error)
                      type: string
                    zotRegistry:
                      description: ZotRegistry tool status
                      properties:
                        enabled:
                          description: Enabled indicates if the tool is enabled
                          type: boolean
                        namespace:
                          description: Namespace where the tool is installed
                          type: string
                        status:
                          description: Status of the tool (Pending, Installing, Ready, Error)
                          type: string
                        trustDomain:
                          description: |-
                            TrustDomain is the SPIFFE trust domain reported by the tool. This field is
                            generic on the shared PlatformToolStatus struct but is only ever populated
                            for the mesh tool, where it carries the pool-scoped trust domain
                            ({pool}.{org}.mesh.kaos-labs.org) read back from the XNetworkMesh XR's
                            status — never recomputed by the operator (DEC-C1).
                          type: string
                        url:
                          description: URL is the access URL for the tool (if applicable)
                          type: string
                        version:
                          description: Version of the tool
                          type: string
                      type: object
                  type: object
                reconciliationAttempts:
                  description: ReconciliationAttempts tracks the number of reconciliation attempts for backoff
                  type: integer
                region:
                  description: |-
                    Region is the cloud region where this cluster is provisioned.
                    RETAINED (not moved to Outputs) despite being a §3.2 MOVE candidate: a
                    cross-CR reader in kubeproject/phases/reconciling_ml.go (resolveRegion)
                    already carries a documented deliberate-non-migration rationale ("Same
                    reasoning applies to resolveRegion" as resolveCloudKey above it) predating
                    this MOVE pass — it deliberately tolerates a not-yet-Ready or
                    mid-deletion pool, which parentref.GetKubePoolOutputs would fail-closed
                    on. Outputs.Region is still filled from this field (Reporting).
                  type: string
                renderedGeneration:
                  description: |-
                    RenderedGeneration is the generation whose spec the last completed
                    Reconciling pass rendered into child XRs. Internal bookkeeping for the
                    observedGeneration attestation on the Ready transition (plan §3.1); NOT
                    an output — no other controller may read it.
                  format: int64
                  type: integer
                resourceSummary:
                  description: ResourceSummary contains a summary of composed resource counts
                  properties:
                    pending:
                      description: Pending is the number of pending resources
                      type: integer
                    ready:
                      description: Ready is the number of ready resources
                      type: integer
                    total:
                      description: Total number of resources
                      type: integer
                  type: object
                secureConnection:
                  description: SecureConnection contains the secure interconnect status when the feature is enabled.
                  properties:
                    message:
                      description: Message carries detail when not Ready (e.g. the unready composed resource).
                      type: string
                    ready:
                      description: Ready indicates the interconnect (XInterconnect XR) is Ready.
                      type: boolean
                    state:
                      description: 'State is a human-readable rollup: Pending | Ready | Degraded | Error | Disabled.'
                      type: string
                  type: object
                subnets:
                  description: Subnets contains categorized subnet IDs (public/private)
                  properties:
                    private:
                      description: Private subnet IDs
                      items:
                        type: string
                      type: array
                    public:
                      description: Public subnet IDs
                      items:
                        type: string
                      type: array
                  type: object
                systemNodeGroups:
                  description: SystemNodeGroups contains status of system node groups
                  items:
                    description: SystemNodeGroupStatus contains the status of a system node group
                    properties:
                      arn:
                        description: ARN of the node group
                        type: string
                      instanceTypes:
                        description: InstanceTypes used by the node group
                        items:
                          type: string
                        type: array
                      name:
                        description: Name of the node group
                        type: string
                      scalingConfig:
                        description: ScalingConfig contains the scaling configuration
                        properties:
                          desiredSize:
                            description: DesiredSize is the desired number of nodes
                            type: integer
                          maxSize:
                            description: MaxSize is the maximum number of nodes
                            type: integer
                          minSize:
                            description: MinSize is the minimum number of nodes
                            type: integer
                        type: object
                      status:
                        description: Status of the node group
                        type: string
                    type: object
                  type: array
              type: object
          type: object
      served: true
      storage: true
      subresources:
        status: {}
{{- end -}}
