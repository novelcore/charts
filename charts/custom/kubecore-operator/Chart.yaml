apiVersion: v2
name: kubecore-operator
description: A sophisticated Kubernetes operator that implements a four-layer framework architecture for provisioning and managing cloud infrastructure through Crossplane compositions.
type: application
version: 0.1.56
appVersion: "0.1.56"

# Maintainers
maintainers:
- name: Novelcore Platform Team
  email: platform@novelcore.io

# Keywords for chart discovery
keywords:
- kubernetes
- operator
- crossplane
- infrastructure
- gitops
- platform
- kubecore
- infrastructure-as-code

# Home page
home: https://github.com/novelcore/kubecore-operator

# Source code
sources:
- https://github.com/novelcore/kubecore-operator

# Icon
icon: "https://github.com/novelcore/kubecore-operator/raw/main/docs/icon.png"

# Annotations for additional metadata
annotations:
  # Category for Artifact Hub
  artifacthub.io/category: automation
  artifacthub.io/license: Apache-2.0
  
  # Links for Artifact Hub
  artifacthub.io/links: |
    - name: GitHub Repository
      url: https://github.com/novelcore/kubecore-operator
    - name: Documentation
      url: https://github.com/novelcore/kubecore-operator/blob/main/README.md
    - name: Release Notes
      url: https://github.com/novelcore/kubecore-operator/releases

  # Changes for this chart version
  artifacthub.io/changes: |
    - kind: changed
      description: "Implemented orchestrated 18-step deletion sequence across 3 phases for robust infrastructure cleanup"
    - kind: added
      description: "XEKS composition now orphans critical resources (Cluster, ClusterAuth, NodeGroup) via deletionPolicy: Orphan"
    - kind: added
      description: "XKubeSystem finalizer ensures Helm Releases properly uninstall before cluster deletion (prevents orphaned Load Balancers)"
    - kind: fixed
      description: "Resolved race condition preventing Traefik from deleting AWS Load Balancers during cluster teardown"
    - kind: changed
      description: "Manual orchestration of orphaned resource deletion prevents in-use blocking and ensures proper AWS cleanup sequence"

# Kubernetes version compatibility
kubeVersion: ">=1.20.0-0"

# Dependencies
dependencies:
- name: crossplane
  version: "2.1.0"
  repository: "https://charts.crossplane.io/stable"
  condition: crossplane.enabled
- name: external-secrets
  version: "1.0.0"
  repository: "https://charts.external-secrets.io"
  condition: externalSecrets.enabled
