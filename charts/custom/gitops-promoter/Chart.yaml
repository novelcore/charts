apiVersion: v2
name: gitops-promoter
description: A Helm chart for GitOps Promoter - A tool for promoting changes between environments in GitOps workflows
type: application

# Chart version - follows semantic versioning
version: 0.5.0

# Application version - matches the GitOps Promoter version
appVersion: "0.14.0"

# Maintainers
maintainers:
- name: Novelcore Platform Team
  email: platform@novelcore.io

# Keywords for chart discovery
keywords:
- gitops
- promotion
- argocd
- continuous-deployment
- environment-promotion

# Home page
home: https://github.com/argoproj-labs/gitops-promoter

# Source code
sources:
- https://github.com/argoproj-labs/gitops-promoter

# Annotations for additional metadata
annotations:
  # Category for Artifact Hub
  artifacthub.io/category: integration-delivery

  # Links for Artifact Hub
  artifacthub.io/links: |
    - name: GitOps Promoter Documentation
      url: https://github.com/argoproj-labs/gitops-promoter/blob/main/README.md
    - name: Release Notes
      url: https://github.com/argoproj-labs/gitops-promoter/releases/tag/v0.14.0

  # Changes for this chart version
  artifacthub.io/changes: |
    - kind: changed
      description: "BREAKING: Updated ControllerConfiguration spec to v0.14.0 format with workQueue configurations for all controllers"
    - kind: changed
      description: "BREAKING: Replaced old duration-based config with new nested workQueue structure (maxConcurrentReconciles, rateLimiter, requeueDuration)"
    - kind: fixed
      description: Fixed PullRequest controller crash by adding required workQueue configuration section
    - kind: changed
      description: Updated CRDs to v0.14.0 schema with new controller-gen v0.19.0 definitions
    - kind: changed
      description: Simplified controller-configuration.yaml template to use toYaml for nested structures
    - kind: added
      description: Added workQueue configuration for argocdCommitStatus, changeTransferPolicy, commitStatus, promotionStrategy, and pullRequest controllers
  # - kind: changed
  #   description: CRDs now installed from crds/ directory for proper Helm lifecycle management
  # - kind: removed
  #   description: Removed CRD template in favor of standard Helm CRD installation pattern
  # - kind: added
  #   description: Initial Helm chart for GitOps Promoter v0.10.2

  # License
  artifacthub.io/license: Apache-2.0

  # Images used by this chart
  artifacthub.io/images: |
    - name: gitops-promoter
      image: quay.io/argoprojlabs/gitops-promoter:v0.14.0
    - name: kube-rbac-proxy
      image: quay.io/brancz/kube-rbac-proxy:v0.17.0

# Kubernetes version compatibility
kubeVersion: ">=1.20.0-0"
