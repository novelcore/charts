{{- if .Values.rbac.helpers.enable }}
apiVersion: rbac.authorization.k8s.io/v1
{{- if .Values.rbac.namespaced }}
kind: Role
{{- else }}
kind: ClusterRole
{{- end }}
metadata:
{{- if .Values.rbac.namespaced }}
  namespace: {{ .Release.Namespace }}
{{- end }}
  labels:
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app.kubernetes.io/name: {{ include "kaos-osr-edge-operator.name" . }}
    helm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  name: {{ include "kaos-osr-edge-operator.resourceName" (dict "suffix" "knowledgesynccontract-admin-role" "context" $) }}
rules:
- apiGroups:
  - osr.kaos.kubecore.io
  resources:
  - knowledgesynccontracts
  verbs:
  - '*'
- apiGroups:
  - osr.kaos.kubecore.io
  resources:
  - knowledgesynccontracts/status
  verbs:
  - get
{{- end }}
